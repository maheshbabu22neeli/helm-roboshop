# kubernetes Helm Version
apiVersion: v1

# Name of the chart/mostly app name
name: redis

# This is the chart version
version: 0.0.1

# This the image or app version
appVersion: 7.0

description: "Redis Helm Chart"