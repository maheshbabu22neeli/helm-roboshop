# Config Map
apiVersion: v1
kind: ConfigMap
metadata:
  name: shipping
  namespace: roboshop
  labels:
    tier: backend
    project: rosboshop
    component: shipping

data:
  CART_ENDPOINT: {{ .Values.configMap.CART_ENDPOINT }}
  DB_HOST: {{ .Values.configMap.DB_HOST }}