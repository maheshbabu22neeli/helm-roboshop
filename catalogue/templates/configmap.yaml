# Config Map
apiVersion: v1
kind: ConfigMap
metadata:
  name: catalogue
  namespace: roboshop
  labels:
    tier: backend
    project: rosboshop
    component: catalogue

data:
  MONGO: "true"
  MONGO_URL: {{ .Values.configMap.mongoUrl }}
